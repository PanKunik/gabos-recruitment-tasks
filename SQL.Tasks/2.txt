WITH Daty AS (
	SELECT CAST('2020-01-01' AS DATE) AS Data
	UNION ALL
	SELECT DATEADD(DAY, 1, Data) AS Data
	FROM Daty D
	WHERE D.Data < CAST('2020-12-31' AS DATE)
)

SELECT * FROM Daty
OPTION (MAXRECURSION 0);